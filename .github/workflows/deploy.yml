name: Deploy to Server

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Deploy to Server
        run: |
          ssh -T ka522929@${{ secrets.DEPLOY_SERVER }} << 'EOF'
          cd /home/ka522929/sisidev.com.ua/www
          git pull origin master
          ./deploy.sh
          EOF